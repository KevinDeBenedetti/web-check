# Variables d'environnement pour le client web

# API Backend URL
# - En développement local (bun dev): utilise http://localhost:8000 via le proxy Vite
# - En développement Docker (docker-compose up): utilise http://api:8000 via le réseau Docker
# - En production: configuré dans nginx.conf
VITE_API_URL=http://localhost:8000
